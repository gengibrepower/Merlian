## What

<!-- What this PR changes, in a sentence or two. -->

## Why

<!-- Motivation/context. Reference the issue if any (e.g. Closes #12). -->

## How to test

<!-- Steps or commands to verify the behavior. -->

## Checklist

- [ ] `npm test` passes
- [ ] `npm run typecheck` is clean
- [ ] Followed TDD (red test before the implementation)
- [ ] No superfluous comments (only the non-obvious)
- [ ] The core (`core/`) stays pure, no I/O
- [ ] The wire contract is unbroken — or, if broken, it is under a new path version
- [ ] Docs updated where needed (`README.md`, `docs/`)
